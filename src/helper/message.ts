export enum EGeneral {
  "TOO_SHORT" = "too short",
  "IN_VALID" = "invalid",
}

export enum EUser {
  "MIN_LENGTH" = "minimum length is 6 character",
  "MAX_LENGTH" = "maximum length is 255 character",
  "PHONE" = "valid phone",
}
